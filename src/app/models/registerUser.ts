export interface RegisterUser {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  password: string;
  confirmPassword: string;
  birthDate: Date;
  city: string;
  profileImageUrl: string;
}
